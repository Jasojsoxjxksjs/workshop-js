//Q1

// Declare two numeric variables
//let num1 = 10;
//let num2 = 3;

// Add the two numbers and print the result
//let sum = num1 + num2;
//console.log("Sum: " + sum);

// Subtract the second number from the first and print the result
//let difference = num1 - num2;
//console.log("Difference: " + difference);

// Multiply the two numbers and print the result
//let product = num1 * num2;
//console.log("Product: " + product);

// Divide the first number by the second and print the result
//let quotient = num1 / num2;
//console.log("Quotient: " + quotient);

// Find the remainder when the first number is divided by the second and print the result
//let remainder = num1 % num2;
//console.log("Remainder: " + remainder);






//Q2




        // JavaScript to add two numbers and display the result
       // function addNumbers() {
            // Declare two variables
         //   let num1 = 10;
           // let num2 = 5;

            // Add the two numbers
            //let sum = num1 + num2;

            // Display the result on the webpage
            //document.getElementById("result").textContent = "The sum is: " + sum;
        //}








//Q3










        // JavaScript program to calculate total cost of an order including tax

// Define the order items with their names, quantities, and prices
//let orderItems = [
  //  { name: "Pizza", quantity: 2, price: 20 },
    //{ name: "Cola", quantity: 3, price: 5 },
    //{ name: "Water", quantity: 4, price: 1.5 }
//];

// Define the tax rate
//let taxRate = 0.15; // 15% tax

// Initialize total cost before tax
//let totalBeforeTax = 0;

// Print the order details to the console
//console.log("Restaurant Order:");
//orderItems.forEach(item => {
    //let itemTotal = item.quantity * item.price;
    //console.log(`${item.name}: ${item.quantity} x $${item.price} = $${itemTotal.toFixed(2)}`);
   // totalBeforeTax += itemTotal;
//});

// Calculate the tax and the final total
//let taxAmount = totalBeforeTax * taxRate;
//let totalCost = totalBeforeTax + taxAmount;

// Print the totals to the //console
//console.log(`\nTotal (before tax): $${totalBeforeTax.toFixed(2)}`);
//console.log(`Tax (15%): $${taxAmount.toFixed(2)}`);
//console.log(`----------------`);
//console.log(`Total Cost: $${totalCost.toFixed(2)}`);
//console.log("Thank you for your order!");

